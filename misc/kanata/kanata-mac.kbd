(defcfg
  process-unmapped-keys yes
  concurrent-tap-hold   yes
)

(defsrc
        esc  f1   f2   f3   f4   f5   f6   f7   f8   f9    f10  f11  f12
        grv  1    2    3    4    5    6    7    8    9     0    -    =    bspc
        tab  q    w    e    r    t    y    u    i    o     p    [    ]    \
        caps a    s    d    f    g    h    j    k    l     ;    '    ret
        lsft z    x    c    v    b    n    m    ,    .     /    rsft
        lctl lalt lmet           spc            rmet ralt)

(defalias
  cap (tap-hold-press 200 200 esc (layer-while-held modal))
  tab (tap-hold-press 200 200 tab (layer-while-held nav))
  hypera M-A-C-S-a
  hyperb M-A-C-S-b
  hyperf M-A-C-S-f
  hyperm M-A-C-S-m
  hypert M-A-C-S-t
  hypero M-A-C-S-o
  hyperz M-A-C-S-z
  hypersc M-A-C-S-;
  hyperqt M-A-C-S-'
  )

(deflayer qwerty
        esc  🔅    🔆   f3   f4   f5   f6   ◀◀   ▶⏸   ▶▶    🔇   🔉   🔊
        grv  1    2    3    4    5    6    7    8    9     0    -    =    bspc
        @tab  q    w    e    r    t    y    u    i    o     p    [    ]    \
        @cap a    s    d    f    g    h    j    k    l     ;    '    ret
        lsft z    x    c    v    b    n    m    ,    .     /    rsft
        lctl lalt lmet           spc            rmet ralt)

(deflayer modal
        lrld  🔅    🔆   f3   f4   f5   f6   ◀◀   ▶⏸   ▶▶    🔇   🔉   🔊
        grv  1    2    3    4    5    6    7    8    9     0    -    =    bspc
        tab  q    w    e    r    t    home    pgdn    pgup    end     p    [    ]    \
        XX lmet    lalt    lsft    lctl    g    lft    down    up    rght     ;    '    ret
        lsft z    x    c    v    b    n    m    ,    .     /    rsft
        lctl lalt lmet           spc            rmet ralt)

(deflayer nav
        esc  🔅    🔆   f3   f4   f5   f6   ◀◀   ▶⏸   ▶▶    🔇   🔉   🔊
        grv  1    2    3    4    5    6    7    8    9     0    -    =    bspc
        XX  q    w    e    r    @hypert    y    u    i    @hypero     p    [    ]    \
        caps @hypera    s    d    @hyperf    g    h    j    k    l     @hypersc    @hyperqt    ret
        lsft @hyperz    x    c    v    @hyperb    n    @hyperm    ,    .     /    rsft
        lctl lalt lmet           spc            rmet ralt)
