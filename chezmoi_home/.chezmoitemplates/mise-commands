# Add neovim plugin (required for neovim support)
mise plugins add neovim || true

# Install/update global tools via mise
mise use --global python@latest
mise use --global node@lts
mise use --global neovim@nightly
mise use --global lua-language-server@latest
mise use --global marksman@latest

# Ensure all configured tools are installed
mise install
